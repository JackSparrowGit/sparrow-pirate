# spring服务名称
spring:
  application:
    name: ${APPLICATION_NAME:sparrow-pirate-eureka}


# app info
info:
  app:
    name: ${spring.application.name}
    description: ${spring.application.name} Application
    version: @project.version@
  project:
    version: @project.version@
